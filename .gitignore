# scratch/test artifacts (selftest uses mktemp; nothing should land here)
*.tmp
.DS_Store
__pycache__/
*.pyc
.pytest_cache/

# a project's own overlay is private to that project (the shipped template is not)
overlays/*
!overlays/_template/
