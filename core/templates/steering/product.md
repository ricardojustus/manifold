<!--
  product.md — steering document (1 of 3: product / tech / structure).
  Pattern adapted from Pimzino/claude-code-spec-workflow (MIT).

  WHAT THIS IS. A durable, per-project statement of WHAT this project is and WHY it exists —
  the stable product context that almost every brief, spec, and vision re-derives if it is not
  written down once. Loaded once per project (not per dispatch); the GIVEN block of a brief
  CITES this instead of restating it. Keep it short and stable: this changes when the product's
  purpose changes, not every sprint. If it drifts stale, fix it here — one source of truth.
  Delete these comments as you fill each section.
-->

# Product — <project name>

## What it is
<!-- One or two plain sentences: what this project is, in language a capable newcomer
     understands without the codebase open. Name the thing, not the architecture. -->

## Who it is for
<!-- The users / operators / callers this serves. Be concrete about who, and what they are
     trying to get done — the job the product does for them. -->

## Why it exists
<!-- The problem it solves and why that problem is worth solving. What was missing or broken
     before this existed. This is the "so what" a vision must stay true to. -->

## What "done" looks like
<!-- The definition of success at the product level — the observable outcomes that mean this is
     working. Not a task list: the falsifiable conditions a third party could check. Per-build
     acceptance criteria live in each Vision; this is the standing bar they inherit. -->

## Explicitly out of scope
<!-- What this product is deliberately NOT — the tempting adjacent things it will not become.
     Naming the non-goals here stops scope creep from re-arguing them per build. -->
