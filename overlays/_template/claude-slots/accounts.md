<!-- FILL accounts: the accounts + config dirs the agent may be launched under — one row per
     login (wrapper/command, which account it authenticates as, which config dir it uses), plus
     anything that is shared across sessions on the same account (quota, rate limits) and any
     directory name that must never be renamed. May be left empty: a project driven from a
     single account has nothing to record here. (The scaffold provides the heading.) -->
