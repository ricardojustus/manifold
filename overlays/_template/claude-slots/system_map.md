<!-- FILL system_map: the concrete layout of THIS project — where the code lives, the
     runtime, docs/notes/state, and any legacy or off-limits systems the agent must know
     about and not touch. (Do NOT repeat the `## System Map` heading — the scaffold provides
     it.) If you declare never-touch systems here: only surfaces owned by NO workstream,
     ownership verified with the owner; back them per ENFORCEMENT.md invariant #4 (prose +
     permission rules / classifier rules). -->
