<!-- FILL user_import: the operator profile — typically a single @-import line pointing at
     the project's USER profile file (who the human is, how they work, how they want to be
     communicated with) so it stays canonical in one place. -->
