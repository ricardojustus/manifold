<!-- FILL compact_instructions: what a post-compaction future-self must re-read and must not
     assume. Add your own `## <heading>` (the scaffold has none before this slot). Name the
     checkpoint file to load, the load-bearing sources to re-read VERBATIM, and the fact that
     skill/rule bodies are dropped post-compaction (re-invoke, don't assume). Optionally wire
     a SessionStart(compact) hook. Leave empty if the project has no compaction workflow. -->
