<!-- FILL identity: the top-level `# <Name>` heading + who this agent is: its name, role,
     relationship to the operator and any sibling agents, and a one-paragraph statement of
     its stance (what it is FOR). This opens the constitution. An empty file is a valid fill,
     but identity is almost always worth writing. -->
